version: '3.1'

services:
  db:
    image: mongo
    volumes:
      - dbstore:/data/db
    networks:
      - auth
  web:
    build: .
    ports:
      '80:80'
    networks:
      - auth

volumes:
  dbstore:

networks:
  auth:
